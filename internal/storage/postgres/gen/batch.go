// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: batch.go

package gen

import (
	"context"
	"errors"
	"time"

	"github.com/jackc/pgx/v5"
	decimal "github.com/shopspring/decimal"
)

var (
	ErrBatchAlreadyClosed = errors.New("batch already closed")
)

const insertOIHistory = `-- name: InsertOIHistory :batchexec
INSERT INTO oi_history (symbol, ts, oi, oi_value_usd)
VALUES ($1, $2, $3, $4)
ON CONFLICT (symbol, ts) DO NOTHING
`

type InsertOIHistoryBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertOIHistoryParams struct {
	Symbol     string
	Ts         time.Time
	Oi         decimal.Decimal
	OiValueUsd decimal.Decimal
}

// T1 inserts ~400 symbols × 10 limit = ~4000 rows per 5-min tick. The batch
// variant lets sqlc generate a typed wrapper that pgx sends in one round-trip.
// ON CONFLICT DO NOTHING handles overlap from `limit=10` covering recent
// ticks (every fetch revisits the last 50 minutes).
func (q *Queries) InsertOIHistory(ctx context.Context, arg []InsertOIHistoryParams) *InsertOIHistoryBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.Symbol,
			a.Ts,
			a.Oi,
			a.OiValueUsd,
		}
		batch.Queue(insertOIHistory, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertOIHistoryBatchResults{br, len(arg), false}
}

func (b *InsertOIHistoryBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertOIHistoryBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}
