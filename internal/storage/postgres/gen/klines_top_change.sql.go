// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: klines_top_change.sql

package gen

import (
	"context"

	decimal "github.com/shopspring/decimal"
)

const getKlinesPriceChangeTop = `-- name: GetKlinesPriceChangeTop :many
WITH ranked AS (
    SELECT symbol, close,
           ROW_NUMBER() OVER (PARTITION BY symbol ORDER BY open_time DESC) AS newest_rn,
           ROW_NUMBER() OVER (PARTITION BY symbol ORDER BY open_time ASC)  AS oldest_rn
    FROM klines
    WHERE timeframe = '15m' AND open_time > NOW() - INTERVAL '24 hours'
)
SELECT
    n.symbol,
    ((n.close - o.close) / o.close)::NUMERIC AS pct_change
FROM ranked n
JOIN ranked o ON n.symbol = o.symbol
WHERE n.newest_rn = 1 AND o.oldest_rn = 1 AND o.close > 0
ORDER BY pct_change DESC NULLS LAST
LIMIT $1
`

type GetKlinesPriceChangeTopRow struct {
	Symbol    string
	PctChange decimal.Decimal
}

// T4 source C: top symbols by 24h price change. For each symbol pairs
// the newest 15m close with the oldest 15m close in the last 24h window;
// on cold-start (< 24h of data) this naturally falls back to the oldest
// bar available (SPEC §6 fallback semantic).
func (q *Queries) GetKlinesPriceChangeTop(ctx context.Context, limit int32) ([]GetKlinesPriceChangeTopRow, error) {
	rows, err := q.db.Query(ctx, getKlinesPriceChangeTop, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetKlinesPriceChangeTopRow
	for rows.Next() {
		var i GetKlinesPriceChangeTopRow
		if err := rows.Scan(&i.Symbol, &i.PctChange); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
